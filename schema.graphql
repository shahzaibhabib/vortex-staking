type Vortex @entity {
  # vortex contract address
  id: ID!
  # list of portal addresses
  portals: [String!]
}

type Portal @entity {
  # portal address
  id: ID!
  # creator's address of the portal
  creator: String!
}
